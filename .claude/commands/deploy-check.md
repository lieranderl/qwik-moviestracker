---
description: Review runtime and deployment changes for Bun SSR, Docker, Cloud Build, and Cloud Run in this repository.
argument-hint: [scope]
model: haiku
---

# Deploy Check

Review deployment and runtime changes in this repository.

## Instructions

1. Use the `deploy-reviewer` agent.
2. Focus on Bun SSR entrypoints, Docker assumptions, Cloud Build, Cloud Run, and
   adapter mismatches.
3. Flag when a full `bun run build` is mandatory.
